#include <stdio.h>
int main(){
char ch;
int a;


printf("Enter a letter");
scanf("%c",&ch);
scanf("%d",&ch);
ch=a;
if((ch>=65) && (ch<=97)){
    printf("It is uppercase");
}
else((ch>=98) && (ch<=123)){
    printf("It is lowercase");
}


}